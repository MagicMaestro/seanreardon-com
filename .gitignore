# Build outputs
dist/
.astro/

# Dependencies
node_modules/

# Environment
.env
.env.production
.env.development
.env.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# AI-search aggregated query logs (decision 8 — no IP / session / identifier
# data ever written, but the log file itself stays out of git as a matter
# of hygiene; logs are operational state, not source)
logs/

# Migration / scratch staging dir. Holds legacy images pulled from the server
# during the projects-page migration (~5 MB) and the resulting CF Images URL
# mapping JSON. Both are derived data — never committed.
tmp/

# OS / IDE
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Astro / managed-host artifacts
.netlify/
.vercel/

# Other
.cache/

# Design source variant backups (parenthetical-suffix saves Sean uses for scratch/variant states)
design/fullLogo-2026-05-04(copper-edge-colors-base-point).psd
